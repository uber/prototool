lint:
  rules:
    remove:
      - REQUEST_RESPONSE_TYPES_UNIQUE
generate:
  go_options:
    import_path: github.com/uber/prototool/internal/cmd/testdata/grpc
  plugins:
    - name: go
      type: go
      flags: plugins=grpc
      output: gen/grpcpb
